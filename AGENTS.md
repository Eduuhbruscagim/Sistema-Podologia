# Global Guidelines - RTK Token Optimization

Always prefix terminal and shell commands with `rtk` (e.g. `rtk git status`, `rtk git diff`, `rtk npm test`, `rtk npm run check`) to minimize token consumption.
